class AccountHolder {

    // Constructor to initialize the object with the given parameters
    constructor(userName,name, hasCheckings, hasSavings, checkingsBalance, savingsBalance) {
      this.name = name; 
      this.hasCheckings = hasCheckings; 
      this.hasSavings = hasSavings; 
      this.checkingsBalance = checkingsBalance; 
      this.savingsBalance = savingsBalance; 

    }
  }

  export default AccountHolder;